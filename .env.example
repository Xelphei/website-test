# Google Calendar API Configuration (build-time only — never shipped to client)
# These are used by scripts/fetch-events.js to generate a static events.json
GOOGLE_CALENDAR_API_KEY=YOUR_API_KEY
GOOGLE_CALENDAR_ID=YOUR_CALENDAR_ID@group.calendar.google.com

# Contact Form Configuration (client-side — VITE_ prefix required)
# FormSubmit.co email: submissions go to this address (see formsubmit.co)
# You must confirm your email the first time a submission is sent.
VITE_CONTACT_EMAIL=your-email@example.com
# Optional: CC additional emails (comma-separated)
VITE_CONTACT_CC_EMAILS=
